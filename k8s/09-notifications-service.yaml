apiVersion: apps/v1
kind: Deployment
metadata:
  name: notifications-service
  namespace: 0debt
  labels:
    app: notifications-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: notifications-service
  template:
    metadata:
      labels:
        app: notifications-service
    spec:
      containers:
      - name: notifications-service
        image: ghcr.io/0debt/notifications-service:latest
        ports:
        - containerPort: 3000
        envFrom:
        - configMapRef:
            name: 0debt-config
        - secretRef:
            name: 0debt-secrets
        env:
        - name: MONGODB_DB_NAME
          value: "notifications_service"
        - name: DATABASE_URL
          value: "mongodb://mongo:27017/notifications_service"
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: notifications-service
  namespace: 0debt
spec:
  selector:
    app: notifications-service
  ports:
  - port: 3000
    targetPort: 3000
