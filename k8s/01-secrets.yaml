apiVersion: v1
kind: Secret
metadata:
  name: 0debt-secrets
  namespace: 0debt
type: Opaque
stringData:
  # JWT Secret - hardcodeado para desarrollo local
  JWT_SECRET: "0debt-local-dev-jwt-secret-2024"

  # === APIS EXTERNAS (opcional - la app funciona sin ellas) ===
  # Email (Resend) - sin esto no se envian emails
  RESEND_API_KEY: "re_123456789"

  # Storage avatares (Supabase) - sin esto no hay avatares
  SUPABASE_URL: "https://placeholder-url.supabase.co"
  SUPABASE_ANON_KEY: "dummy-key"
  SUPABASE_BUCKET: "avatars"

  # Imagenes grupos (Unsplash) - sin esto no hay covers
  UNSPLASH_ACCESS_KEY: ""
