apiVersion: apps/v1
kind: Deployment
metadata:
  name: groups-service
  namespace: 0debt
  labels:
    app: groups-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: groups-service
  template:
    metadata:
      labels:
        app: groups-service
    spec:
      containers:
      - name: groups-service
        image: ghcr.io/0debt/groups-service:latest
        ports:
        - containerPort: 3000
        envFrom:
        - configMapRef:
            name: 0debt-config
        - secretRef:
            name: 0debt-secrets
        env:
        - name: MONGODB_DB_NAME
          value: "groups_service"
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: groups-service
  namespace: 0debt
spec:
  selector:
    app: groups-service
  ports:
  - port: 3000
    targetPort: 3000
