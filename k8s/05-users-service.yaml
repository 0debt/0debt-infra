apiVersion: apps/v1
kind: Deployment
metadata:
  name: users-service
  namespace: 0debt
  labels:
    app: users-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: users-service
  template:
    metadata:
      labels:
        app: users-service
    spec:
      containers:
      - name: users-service
        image: ghcr.io/0debt/users-service:latest
        ports:
        - containerPort: 3000
        envFrom:
        - configMapRef:
            name: 0debt-config
        - secretRef:
            name: 0debt-secrets
        env:
        - name: MONGODB_DB_NAME
          value: "users_service"
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: users-service
  namespace: 0debt
spec:
  selector:
    app: users-service
  ports:
  - port: 3000
    targetPort: 3000
